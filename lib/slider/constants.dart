import 'package:flutter/material.dart';

const int fadeInTimeMs = 500;
const int shuffleTimeMs = 140;
const int hoverTimeMs = 400;

const Color pinkColor = Color.fromARGB(0, 255, 254, 255);

const double iconSize = 60;
const Color iconColor = Colors.lightBlue;

const Color tileColor = Color(0x08000000);
